import React,{useState} from "react"; export default function AddMemberForm({onAddMember}){const[name,setName]=useState("");const[role,setRole]=useState("");function submit(e){e.preventDefault();if(!name.trim()||!role.trim())return;onAddMember({name:name.trim(),role:role.trim(),email:name.trim().toLowerCase().replace(/\s+/g,".")+"@example.com",projects:0,status:"Available"});setName("");setRole("")}return <section className="panel"><span className="eyebrow">CALLBACK PROP</span><h2>Add a member</h2><p>Child sends data to the parent through <code>onAddMember</code>.</p><form onSubmit={submit}><input value={name} onChange={e=>setName(e.target.value)} placeholder="Member name" required/><input value={role} onChange={e=>setRole(e.target.value)} placeholder="Role" required/><button type="submit">Add Member</button></form></section>}